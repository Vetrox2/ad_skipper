[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
ultralytics = "*"
pure-python-adb = "*"
opencv-python = "*"
pillow = "*"
python-dotenv = "*"
PySide6 = "*"

[dev-packages]

[requires]
python_version = "3.12.2"

[scripts]
start = "python -m src.ad_skipper"
gui = "python -m src.ad_skipper.gui.__main_gui__"
build = "python build.py"
release = "python release.py"
make-ico = "python -c \"from PIL import Image; Image.open('assets/icon.png').save('assets/icon.ico', format='ICO', sizes=[(16,16), (24,24), (32,32), (48,48), (64,64), (128,128), (256,256)])\""
train = "pipenv run yolo detect train data=dataset/data.yaml model=yolov8n.pt epochs=100 imgsz=640"